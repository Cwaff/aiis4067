function prediction = NNTesting(testImage, modelNN)
    size(testImage)
    size(modelNN)
    distance = zeros(2,size(modelNN.neighbours,1));
    for i=1:size(modelNN.neighbours,1)
        
    %prediction = 0;
end